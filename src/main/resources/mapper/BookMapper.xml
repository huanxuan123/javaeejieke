<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.dao.BookMapper">

    <resultMap id="BookResultMap" type="Book">
        <id property="bookId" column="book_id"/>
        <result property="isbn" column="isbn"/>
        <result property="title" column="title"/>
        <result property="author" column="author"/>
        <result property="publisher" column="publisher"/>
        <result property="publishDate" column="publish_date"/>
        <result property="category" column="category"/>
        <result property="price" column="price"/>
        <result property="totalStock" column="total_stock"/>
        <result property="availableStock" column="available_stock"/>
        <result property="location" column="location"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectById" resultMap="BookResultMap">
        SELECT * FROM book WHERE book_id = #{bookId}
    </select>

    <select id="selectByIsbn" resultMap="BookResultMap">
        SELECT * FROM book WHERE isbn = #{isbn}
    </select>

    <select id="selectAll" resultMap="BookResultMap">
        SELECT * FROM book ORDER BY book_id
    </select>

    <select id="selectByCondition" resultMap="BookResultMap">
        SELECT * FROM book
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="author != null and author != ''">
                AND author LIKE CONCAT('%', #{author}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY book_id
    </select>

    <select id="selectAvailableBooks" resultMap="BookResultMap">
        SELECT * FROM book WHERE status = 1 AND available_stock > 0 ORDER BY book_id
    </select>

    <insert id="insert" parameterType="Book" useGeneratedKeys="true" keyProperty="bookId">
        INSERT INTO book (isbn, title, author, publisher, publish_date, category, price, total_stock, available_stock, location, description, status)
        VALUES (#{isbn}, #{title}, #{author}, #{publisher}, #{publishDate}, #{category}, #{price}, #{totalStock}, #{availableStock}, #{location}, #{description}, #{status})
    </insert>

    <update id="update" parameterType="Book">
        UPDATE book
        <set>
            <if test="isbn != null and isbn != ''">isbn = #{isbn},</if>
            <if test="title != null and title != ''">title = #{title},</if>
            <if test="author != null and author != ''">author = #{author},</if>
            <if test="publisher != null and publisher != ''">publisher = #{publisher},</if>
            <if test="publishDate != null">publish_date = #{publishDate},</if>
            <if test="category != null and category != ''">category = #{category},</if>
            <if test="price != null">price = #{price},</if>
            <if test="totalStock != null">total_stock = #{totalStock},</if>
            <if test="availableStock != null">available_stock = #{availableStock},</if>
            <if test="location != null and location != ''">location = #{location},</if>
            <if test="description != null and description != ''">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE book_id = #{bookId}
    </update>

    <delete id="deleteById">
        DELETE FROM book WHERE book_id = #{bookId}
    </delete>

    <update id="updateStock">
        UPDATE book SET available_stock = #{availableStock} WHERE book_id = #{bookId}
    </update>

</mapper>
