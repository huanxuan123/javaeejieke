<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.dao.ReaderMapper">

    <resultMap id="ReaderResultMap" type="Reader">
        <id property="readerId" column="reader_id"/>
        <result property="studentNo" column="student_no"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="department" column="department"/>
        <result property="borrowLimit" column="borrow_limit"/>
        <result property="borrowedCount" column="borrowed_count"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectById" resultMap="ReaderResultMap">
        SELECT * FROM reader WHERE reader_id = #{readerId}
    </select>

    <select id="selectByStudentNo" resultMap="ReaderResultMap">
        SELECT * FROM reader WHERE student_no = #{studentNo}
    </select>

    <select id="selectAll" resultMap="ReaderResultMap">
        SELECT * FROM reader ORDER BY reader_id
    </select>

    <select id="selectByCondition" resultMap="ReaderResultMap">
        SELECT * FROM reader
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="studentNo != null and studentNo != ''">
                AND student_no = #{studentNo}
            </if>
            <if test="department != null and department != ''">
                AND department = #{department}
            </if>
        </where>
        ORDER BY reader_id
    </select>

    <insert id="insert" parameterType="Reader" useGeneratedKeys="true" keyProperty="readerId">
        INSERT INTO reader (student_no, name, gender, phone, email, department, borrow_limit, borrowed_count, status)
        VALUES (#{studentNo}, #{name}, #{gender}, #{phone}, #{email}, #{department}, #{borrowLimit}, #{borrowedCount}, #{status})
    </insert>

    <update id="update" parameterType="Reader">
        UPDATE reader
        <set>
            <if test="studentNo != null and studentNo != ''">student_no = #{studentNo},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null and gender != ''">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="email != null and email != ''">email = #{email},</if>
            <if test="department != null and department != ''">department = #{department},</if>
            <if test="borrowLimit != null">borrow_limit = #{borrowLimit},</if>
            <if test="borrowedCount != null">borrowed_count = #{borrowedCount},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE reader_id = #{readerId}
    </update>

    <delete id="deleteById">
        DELETE FROM reader WHERE reader_id = #{readerId}
    </delete>

    <update id="updateBorrowedCount">
        UPDATE reader SET borrowed_count = #{borrowedCount} WHERE reader_id = #{readerId}
    </update>

</mapper>
